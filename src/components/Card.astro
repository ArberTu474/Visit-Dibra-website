---
// Props
export interface Props {
  hasPadding?: boolean
}

const { hasPadding = true } = Astro.props
---

<div
  class={`bg-gray-300/30 backdrop-blur-md rounded-xl text-gray-500 border-gray-300/30 border overflow-hidden w-full ${
    hasPadding ? 'p-5' : ''
  }`}
>
  <slot />
</div>
